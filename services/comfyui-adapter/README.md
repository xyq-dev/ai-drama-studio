# @ai-drama/comfyui-adapter

M1-A stub. This process validates an optional `COMFYUI_BASE_URL` and answers health checks. It does not call ComfyUI, load models, read workflows, or connect to the Core database.
