# @ai-drama/providers

M1-A package boundary only.

No mock provider, commercial provider, or vendor SDK is implemented here. Vendor-specific request fields are intentionally absent. Later milestones add adapters behind this package without changing the Core database ownership rules.
